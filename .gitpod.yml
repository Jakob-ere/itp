image:
  file: .gitpod.Dockerfile

tasks:
  - init: sdk use java 17.0.4.tem
    command: cd cryptocojo_backend; mvn compile

ports:
  # used by virtual desktop and vnc, supports JavaFX
  - port: 6080
